const prompt = require('prompt-sync')();

const choose = prompt('Combine or Deconstruct?');




if (choose === 'Combine') {
    const combine = prompt('Enter two colors with a space inbetween: ');
    if (combine = ('red, blue'));
    console.log('purple');
    
}



